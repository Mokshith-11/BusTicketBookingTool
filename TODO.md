# TODO - Angular Build Fix

- [ ] Reclassify Angular runtime packages in `BusTicketBookingTool-Frontend/package.json` (move from devDependencies to dependencies).
- [ ] Fix strict typing issues in `src/app/core/interceptors/api.interceptor.ts`.
- [ ] Fix strict typing issues in `src/app/features/module-endpoints/module-endpoints.component.ts`.
- [ ] Run `npm install` in `BusTicketBookingTool-Frontend`.
- [ ] Run `npm run build` and verify build succeeds.
